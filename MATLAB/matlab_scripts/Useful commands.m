%Useful commands

%[height, width, color] = size(image);
%imshow(image);
%[rowA, colA] = find(image > 200);
%names = cell(L,1);

%[max,I] = max(v);
%[I_row, I_col] = ind2sub(size(bw),I);
%figure(1);
%imshow(bw);
%figure(2);
%imshow(image);


%v = image(:);
%figure(2);
%histogram(image);